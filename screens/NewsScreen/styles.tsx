import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  news: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    width: '100%',
    height: '100%',
    marginTop: 35,
    gap: 16,
  },
})
